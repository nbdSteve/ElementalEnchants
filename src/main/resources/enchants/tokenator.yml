max-level: 250

upgrade:
  type: token
  price: 50

lore: '&eTokenator {level}'

data:
  type: tokenator
  base-rate: 0.00 # starting proc chance
  multiplier: 0.1 # percentage added per level
  message:
    enabled: false # message handled in tokens/messages.yml
    text:
  sound:
    enabled: true
    type: 'villager_yes'
    volume: 1
    pitch: 1