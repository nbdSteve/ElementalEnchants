max-level: 100

upgrade:
  type: prestige
  price: 1

lore: '&6Backpack Buster {level}'

data:
  type: command
  base-rate: 0.05 # starting proc chance
  multiplier: 0.05 # percentage added per level
  base-amount: 50 # base amount of blocks sold
  amount-multiplier: 0.05 # percentage of blocks added per level
  commands:
    - pack sell {player} all {amount}